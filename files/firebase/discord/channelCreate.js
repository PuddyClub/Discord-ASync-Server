module.exports = function (cmd, cfg, index) {
    return new Promise((resolve, reject) => {

        // Channel
        const channel = cmd[0];

        // Complete
        resolve();
        return;

    });
};