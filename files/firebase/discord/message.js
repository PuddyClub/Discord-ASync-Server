module.exports = function (cmd, cfg, index) {
    return new Promise((resolve, reject) => {

        // Message
        const msg = cmd[0];

        // Complete
        resolve();
        return;

    });
};