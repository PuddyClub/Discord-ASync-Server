module.exports = function (cmd, cfg) {
    return new Promise((resolve, reject) => {

        // Firebase
        const db = cfg.bot.firebase.db.main;

        // Complete
        resolve();
        return;

    });
};